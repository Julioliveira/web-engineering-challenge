import React from 'react'
import classes from './Toolbar.css'

const toolbar = () => (
    <header className={classes.Toolbar}>
        <span className={classes.Title}>Toronto Waste Lookup</span>
    </header>
)

export default toolbar